#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies a and b.
 * @a: to multiply with b
 * @b: to multiply with a
 * Return: Always 0.
 */
int mul(int a, int b)
{
int multi = a * b;
return (multi);
}
