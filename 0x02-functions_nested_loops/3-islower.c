#include "main.h"
/**
 * _islower - return 1 if true, 0 if false, fllowd by a new lines
 *@a letter as test
 * _islower return 0 or 1
 */
int _islower(int a)
{
if (a >= 97 && a <= 122)
{
  return (1);
}
 return (0);
}
